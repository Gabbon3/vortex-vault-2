ALTER TABLE refresh_token
alter COLUMN token_hash CHAR(64);

ALTER TABLE refresh_token
ADD CONSTRAINT unique_token_hash UNIQUE (token_hash);

CREATE INDEX idx_token_hash_hash ON refresh_token USING hash (token_hash);


select * from refresh_token;

delete from refresh_token;


select * from public."user" u ;